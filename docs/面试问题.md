# Trip-Planner 智能旅行助手 — 面试一百问

> 基于 Trip-Planner 项目的面试题集，覆盖项目架构、FastAPI、Pydantic、多智能体系统、LLM 集成、外部 API 调用、设计模式、错误处理、项目改造十大方向。

---

## 一、项目整体理解（宏观）

1. 请介绍这个项目的整体功能和架构？多智能体系统是如何协作的？

2. 如果让你重新设计这个项目的架构，你会做哪些改进？

3. 项目的前端和后端是如何通信的？接口设计遵循什么规范？

4. 项目的配置信息是如何管理的？有什么优缺点？

5. 如果需求变成"支持多城市多日游"，架构上需要做什么调整？

6. 项目的后端有没有使用数据库？如果后续需要存储用户历史行程记录，应该如何设计？

---

## 二、FastAPI 框架（路由与中间件）

7. 项目中为什么用 APIRouter 来组织路由？prefix 参数的作用是什么？

8. 在 main.py 中，CORS 中间件是如何配置的？allow_origins、allow_credentials、allow_methods、allow_headers 各自的作用是什么？

9. app.add_middleware(CORSMiddleware, ...) 的底层原理是什么？浏览器在什么情况下会发送 OPTIONS 预检请求？

10. FastAPI 的依赖注入系统是如何工作的？本项目为什么没有使用 Depends，而是手动调用工厂函数？

11. 项目中使用了 @app.on_event("startup") 和 @app.on_event("shutdown")，它们的作用是什么？FastAPI/Lifespan 还有哪些生命周期管理方式？

12. FastAPI 的 docs_url 和 redoc_url 是如何生成 API 文档的？背后用了什么规范？

13. FastAPI 路由中 response_model 参数的作用是什么？如果返回数据与 response_model 不匹配会怎样？

---

## 三、Pydantic 模型与数据校验

14. 项目中用到的 BaseModel、Field、field_validator 分别是什么作用？

15. 在 WeatherInfo 模型中，field_validator('day_temp', 'night_temp', mode='before') 中的 mode='before' 是什么意思？还有其他 mode 吗？

16. Field(..., description="...", examples=[...]) 中的 ... 代表什么？和 Field(default=...) 的区别是什么？

17. Pydantic 的 json_schema_extra 有什么用途？项目中用在了哪里？

18. 如果前端传入了一个模型中不存在的字段，Pydantic 会怎么处理？如何控制这个行为？Config 子类中的 extra 参数有哪些可选值？

19. Pydantic 的序列化和反序列化分别通过什么方法实现？TripPlan(**data) 这种写法内部发生了什么？

20. 项目中的 Attraction、Meal、Hotel 等模型的字段约束（如 ticket_price: int = Field(default=0)）和 LLM 输出的 JSON 是什么关系？

21. TripPlanResponse 为什么设计成 success/message/data 三层结构？统一响应格式有什么好处？

---

## 四、多智能体系统（核心亮点）

22. 三个数据查询 Agent 使用了 ThreadPoolExecutor 并行执行，这和 asyncio.gather 有什么区别？如果改成 asyncio.gather 需要做什么调整？

23. 如果天气 Agent 查询失败（高德 API 返回空数据），Planner Agent 应该如何应对？项目中是怎样处理的？

24. 项目的 Agent 不是让 LLM 去调用其他 Agent，而是主规划器依次调用各 Agent 的 run() 方法。这两种设计各有什么优缺点？

25. Agent 的输出格式是如何约束的？如果 LLM 返回了格式错误的 JSON，项目怎么处理？

26. _parse_response 方法中的 JSON 提取逻辑是怎样的？为什么要支持多种提取方式？

27. _fix_field_names 方法解决的是什么问题？你还遇到过 LLM 输出的哪些常见错误？

28. Planner Agent 的提示词中包含了详细的 JSON 格式示例，这样做有什么好处和风险？

29. 如果新增一个"交通规划 Agent"，应该如何集成到现有系统中？

30. _create_fallback_plan 方法在什么情况下被调用？它生成的备选方案和正常方案有什么区别？

31. 为什么 Attraction、Weather、Hotel 三个 Agent 共用同一个 MyAgentsLLM 实例而不是各自创建？

---

## 五、LLM 集成与调用

32. MyAgentsLLM 类是如何封装 OpenAI SDK 的？为什么选择兼容 OpenAI API 的方案？

33. 项目的 LLM 支持 DeepSeek、OpenAI 等多种模型，是如何实现的？切换模型时只需要改什么配置？

34. ToolAgent.run() 方法的 max_rounds 参数的作用是什么？如果 LLM 陷入死循环一直调工具怎么办？

35. temperature 参数的作用是什么？项目中 invoke_with_tools 默认 0.3，为什么比通用对话低？

36. 项目中 LLM 的 API Key 是从哪里读取的？读取的优先级顺序是什么？

37. 如果 LLM 调用超时，项目中有处理吗？应该怎么处理？

38. OpenAI 客户端的 base_url 参数为什么能让我们切换到 DeepSeek？

39. LLM 的调用是同步的还是异步的？如果并发量大，会有什么问题？如何优化？

40. 提示词工程（Prompt Engineering）在这个项目中体现在哪些地方？

41. invoke_with_tools 方法中的 tool_choice="auto" 是什么意思？如果设为 "none" 或强制指定某个工具名会怎样？

42. LLM 返回的 tool_calls 中，assistant 消息需要包含 tool_calls 字段加入对话历史，为什么不能省略？

43. 项目中为什么把 MyAgentsLLM 和 ToolAgent 拆成两个类，而不是合并在一个类里？

---

## 六、外部 API 集成（高德地图 & Unsplash）

44. AmapService 中 search_poi() 方法调用了高德的哪个 API？参数有哪些？

45. 高德地图 API 返回的经纬度是字符串格式 "116.397,39.907"，项目中是如何解析的？

46. plan_route() 方法支持哪些交通方式？路由类型映射是如何实现的？

47. 天气查询中 extensions=all 参数的作用是什么？不加的话会返回什么？

48. AmapService 中没有 geocode 方法，如果要实现地址转坐标应该怎么设计？

49. UnsplashService 的 API 调用为什么要捕获异常并返回空字符串？这样设计是否合理？

50. 外部 API 调用的超时时间是如何设置的？如果高德 API 响应慢会有什么影响？

51. 项目中所有服务类都用了单例模式，为什么？单例模式的优缺点是什么？

52. 如果高德地图 API 更换了版本，项目需要做哪些修改？如何设计才能让改动最小化？

53. 如何对 AmapService 编写单元测试？需要 Mock 哪些部分？

54. 高德 API 每天有调用次数限制，项目中有什么措施来控制调用量？应该怎么优化？

---

## 七、工具定义与 Function Calling

55. tools/tool.py 中定义的 TOOL_SEARCH_POI 和 TOOL_GET_WEATHER 是什么格式？为什么用这种格式？

56. Function Calling 的 schema 中，parameters 的 required 字段有什么作用？如果 LLM 调工具时缺少必填参数会怎样？

57. create_tool_map 函数的作用是什么？为什么要把工具名映射到实际函数，而不是在 Agent 里直接调用？

58. ATTRACTION_HOTEL_TOOLS 和 WEATHER_TOOLS 为什么要分开定义？放在一起会有什么问题？

59. 如果新增一个"搜索餐厅"的工具，需要在哪些文件中做修改？

60. LLM 调工具返回的结果是 JSON 字符串，Agent 是如何把工具结果继续传给 LLM 的？

---

## 八、设计模式与代码质量

61. 项目中使用了哪些设计模式？请具体说明。

62. 单例模式在项目中的实现方式是什么？线程安全吗？如何改进？

63. get_amap_service() 和 get_unsplash_service() 这种工厂函数模式有什么好处？

64. MultiAgentTripPlanner 的 plan_trip() 方法中，三个 Agent 使用 ThreadPoolExecutor 并行执行。如果改成 asyncio 方式，代码会有什么变化？

65. 项目中是否存在循环依赖？FastAPI 项目如何避免循环依赖？

66. 配置管理使用 pydantic-settings 和 .env 文件，这种方式的优缺点是什么？

67. 日志打印遍布在代码中（print），是否应该用 logging 模块替换？怎么替换？

68. 项目中的魔术数字（如 ThreadPoolExecutor(max_workers=3)、API 超时 10 秒）是否应该提取为常量？

69. _create_fallback_plan 方法的设计体现了什么原则？属于哪种设计模式？

70. 如果你要给这个项目做 Code Review，你会指出哪些问题？

71. 项目中的 print 语句在生产环境有什么问题？如何统一替换为 logging？

72. ToolAgent 类的 run 方法中，如果 tool_map 里找不到对应的工具函数，返回的错误信息是中文硬编码的，这样设计有什么问题？

---

## 九、错误处理与健壮性

73. 项目中有几层错误处理机制？分别是什么？

74. API 路由中的 try-except 和 HTTPException 是怎么配合的？

75. 当 LLM 调用失败时，项目如何保证仍然能返回一个可用的旅行计划？

76. validate_config() 函数中的 errors 和 warnings 有什么区别？为什么要分开？

77. 如果高德地图 API Key 过期了，用户会看到什么？如何改善这个体验？

78. FastAPI 启动时配置验证失败会怎样？项目中的处理方式合理吗？

79. _parse_response 失败后会调用 _create_fallback_plan，这种降级策略是否足够？降级方案应该由谁决定？

80. 如果所有三个数据 Agent 都返回空数据，Planner Agent 会给出什么结果？

81. 如果大量用户同时请求旅行计划，会出现什么问题？如何做限流？

82. 如何为项目添加请求日志和错误追踪（如 Sentry）？

83. ToolAgent.run 中的异常捕获（except Exception as e: result = {"error": str(e)}）吞掉了所有异常，这样有什么风险？应该如何改进？

---

## 十、项目改造与个人贡献

84. 你在这个项目中做了哪些核心改造？请逐一说明动机和实现方式。

85. 原项目使用 MCPTool 框架调用高德 API，你改成了什么方式？为什么这样改？

86. 原项目的工具调用方式（MCPTool）和你现在的方式（Function Calling Schema + lambda 映射）各有什么优缺点？

87. 你手动实现了 MyAgentsLLM 和 ToolAgent 替代 hello-agents 框架的 SimpleAgent，实现过程中遇到了哪些问题？你是怎么解决的？

88. 你把前三个 Agent 从串行改成了 ThreadPoolExecutor 并行执行，响应时间提升了多少？为什么不用 asyncio.gather？

89. 这三个改造中，你觉得哪个最能体现你的能力？为什么？

---

## 参考答案指引

每个问题的答案都可以在项目源码中找到对应依据，关键文件参考：

| 问题领域 | 对应文件 |
|---------|---------|
| FastAPI 路由 | backend/app/api/main.py, backend/app/api/routes/ |
| Pydantic 模型 | backend/app/models/schemas.py |
| 多智能体系统 | backend/app/agents/trip_planner_agent.py |
| LLM 集成 | backend/app/services/llm_service.py |
| 高德地图服务 | backend/app/services/amap_service.py |
| 工具定义 | backend/app/tools/tool.py |
| Unsplash 服务 | backend/app/services/unsplash_service.py |
| 配置管理 | backend/app/config.py, .env |
| 项目启动 | run.py |

---

> 祝面试顺利！
